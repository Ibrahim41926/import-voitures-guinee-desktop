# Windows Packaging

Place `app.ico` in this directory if you want a custom Windows icon for `jpackage`.

Expected path:

```text
packaging/windows/app.ico
```

Build the installer with:

```powershell
.\scripts\build-windows-installer.ps1
```
